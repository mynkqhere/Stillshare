"use client";
import axios from "axios";
import { useState } from "react";
async function Feedcard(){
    const [Posts, setPosts] = useState([]) // state to hold the posts fetched from the backend.
    return(<div></div>)
}
export default Feedcard;